//! Rule-based classification implementations
//!
//! This module contains classifiers that use deterministic rules and patterns
//! rather than machine learning models.

pub use crate::classifier::hybrid::HybridClassifier;
pub use crate::classifier::stub::StubClassifier;
